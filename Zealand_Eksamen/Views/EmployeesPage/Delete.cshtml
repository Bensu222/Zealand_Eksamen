@model Zealand_Eksamen.Models.Employee
@{
    ViewData["Title"] = "Slet Medarbejder";
}
<h2>Slet Medarbejder</h2>

<div class="alert alert-danger">Er du sikker på, at du vil slette @Model.FullName?</div>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="EmployeeID" />
    <button class="btn btn-danger" type="submit">Slet</button>
    <a class="btn btn-secondary" href="@Url.Action("Index")">Annuller</a>
</form>